# MacQuadra 800 performance handoff — 2026-09-21

The target remains an authentic 33 MHz MacQuadra 800 with five valid
Speedometer 4.02 Benchmark Mix runs at or above 1.8. It is not achieved yet.
The best hardware result is P120: Mix median 1.313, mean 1.3124, five valid
runs, zero invalid timers. The real Quadra reference Mix is 1.897. No newer
bitstream has been deployed.

## Current build

P150 is still the one active Quartus flow in this checkout. The wrapper is
`scripts/cpu/fit_cache_response_seed25.sh`; the archive is
`scratch/p150_routing_seed25_fit_20260921`; the wrapper, quartus_sh, and
quartus_fit were still live when this handoff was written. The archive does
not yet contain a terminal `build.exit` or post-fit source check. Do not infer
failure from the old `output_files` reports: those are stale P147 output.

All 157 tracked HDL/QSF/QIP/SDC inputs were rehashed unchanged during the
flow. Do not edit tracked implementation files or start another Quartus flow
until P150 is terminal and its fresh reports are reviewed. Leave unrelated
untracked `worst_detail.txt` and `worst_paths.txt` alone.

## Best simulation candidate

P161 CPU plus P151 shared cache port, with P120 pipeline, is the qualified
combined candidate. The next guarded build is prepared but not launched:
`scripts/cpu/fit_combined_forward_shared_port.sh`, documented in
`docs/P163_COMBINED_FORWARD_SHARED_PORT_FIT_20260921.md`. It uses seed 25 and
the development CDROM_OFF/ETHERNET_OFF settings. Promote it only after the
P150 wrapper is terminal, then commit/push, verify machine-wide Quartus
idleness, and run one flow in this checkout.

P162 adds ordinary multiword FPU read continuation to P161. It is simulation
qualified in the following scope:

- Whetstone: 25,267,804 loop / 25,268,432 returned cycles, a 98,665-cycle
  (0.389%) improvement over P161/P151.
- Dhrystone: 104,254,232 / 104,255,122, unchanged; the independent 50,000
  iteration checker passes.
- 18 non-final ordinary FPU read acknowledgements continue immediately with
  the checked address, index, and operand capture.
- Explicit P151-cache fault suites pass 54 cases across MMU-off, 4K, and 8K
  modes. Ordinary split reads pass for both page sizes; all 3 continuation
  beats continue on P162 and zero on P161.
- P161/P162 full integration logs exist under
  `scratch/p162_full_integration_20260921`; a root audit and source-manifest
  review are still required before calling P162 fully qualified.

P164 is an unqualified follow-on that chains non-final FMOVEM reads. Its
  source is scratch-only and has not been screened or promoted.

## Correctness coverage already pushed

The P161 forwarding change has passed value, source/destination MMU fault,
split-read, trace, and synchronized IRQ-boundary fixtures. Destination-fault
coverage is in `scratch/p161_dest_fault_root_runs_20260921`; IRQ coverage is in
`scratch/p161_irq_pins_runs_20260921`. The internal-only IRQ injection that
violated the testbench phantom-IRQ invariant is explicitly excluded.

## Next actions

1. Let P150 finish; inspect only fresh archived map, fit, STA, cross-domain,
   artifact, and source-after reports.
2. Independently audit P162 full integration and source hashes. If clean,
   record the result and decide whether P163 should use P161 or P162; P162 is
   not currently included in the P163 wrapper.
3. If P150 is unusable, promote the exact chosen candidate, launch P163 once,
   inspect RAM inference and timing, and preserve the fresh RBF/hash.
4. Deploy only from a fresh safe-shutdown screen on `mister.local`. Run five
   valid all-ten Speedometer mixes, report invalid timer runs separately, then
   verify boot, responsiveness, and clean shutdown. Do not claim progress from
   simulation counts as a hardware score.

All progress through commit `264af6d` is pushed to `origin/add-ethernet`.
