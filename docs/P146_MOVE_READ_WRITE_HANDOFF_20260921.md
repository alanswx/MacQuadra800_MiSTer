# P146 successful MOVE read to ordered write handoff

Unqualified isolated P137 core candidate SHA256 `eb2b7b80bc927fdb67bafac49c477cfb4cfdabbd6c05f6c48f7d0da6c4ca0f6b` at `scratch/p146_move_read_write_handoff_20260921/ap040_core.v`; patch `scripts/cpu/move_read_write_handoff.patch`. Use P120 pipeline/P136 cache. Production remains frozen for P147 FPGA fitting.

For ordinary memory-to-memory MOVE with a settled simple destination base, consume the successful source response into the existing ALU and issue the destination write on the same edge. Preserve destination An undo/update ordering. Read errors retain precedence; split source reads and unsettled/extension destination modes keep the existing path. The early request remains restricted to on-board RAM and a within-page destination; other destinations retain delayed issue. No speculative write or new address hint is added.

This changes the core/MMU/cache consecutive-request contract and is not qualified by existing read-only checks. First screen full original workloads and exact captures, then require directed read-to-write exact-once, source/destination faults, same-An updates, partial/page-spanning writes, stalls and integration before any FPGA use. Qualification is scoped below; directed coverage of the new handoff is still required.

Completed P120/P136 workload screens against P137: Whetstone 26,093,404 / 26,094,032 returned, saving 150,367 loop clocks (0.573%); Dhrystone 105,004,226 / 105,005,116 returned, saving 1,050,024 loop clocks (0.990%). Root independently verified all eight captures and every non-core identity; independent Dhrystone checker passes. Initial agent report compared Whetstone to the wrong candidate and then mixed loop/return counts; only the P137 loop comparisons here are accepted. Full integration and directed consecutive read/write/fault checks are now in progress. No FPGA or hardware qualification.

Root integration audit: all 22 CPU programs and four IRQ/replay tests pass, every HANDOFF entry is accounted for by commit or cancellation, the oracle trace matches exactly, and all recorded source hashes match. Evidence: `scratch/p146_full_integration_20260921/root_audit.txt`, using explicit P136 cache and P120 pipeline.

The first additional memory-to-memory tests passed but do not qualify the new shortcut: their default destination is indexed (mode 6), whereas P146 requires modes 2/3/4. Their monitors count source acknowledgements rather than actual early write issue. Their compiled executables also name the production cache, now P147, despite the initial agent report claiming explicit P136. Preserve these as fallback regression evidence only. Directed simple-destination, stable early-issue, exact-once, fault, alias, partial-write and stall checks remain pending; no FPGA promotion is authorized by these results alone.

Root reviewed actual handoff coverage in `p{137,146}_rootactual_s{2,3,4}/run.log`: each P146 simple destination mode passes the architectural matrix and records 324 early handoffs, versus zero for P137. The preserved `scripts/cpu/move_read_write_handoff_monitor.sv` samples successful ordinary MOVE source-read context before the edge, then requires registered S_MWR/m_issued/mem_req/write after the edge. Candidate runs require nonzero coverage at the success marker. Earlier `exact_s*` counters counted all held S_MWR cycles and are excluded as early-issue or exact-once evidence. Per-instruction write-acknowledgement accounting and fault-path coverage remain pending.

Root audited `p{137,146}_exact_s{2,3,4}`: architectural checks pass, compiled files name explicit P136 cache, and each P146 run reports `early=324 checked=324` (P137 0/0). The preserved `move_read_write_exact_monitor.sv` associates the early handoff with its instruction PC and requires exactly one successful core write acknowledgement before the PC changes, rejecting duplicates and overlap. This proves core acknowledgement accounting for these successful fixtures, not independent downstream physical-write counts. Fault-path early coverage remains a separate gate.

Root audited corrected per-case fault runs in `p{137,146}_faultactual_s{2,3,4}`: all 36 logs pass without FAIL/FATAL/ERROR. Each log covers three bus phases. Source, source-postincrement and source-predecrement faults report early0 on both cores. Destination faults with each source update form report P146 early3 total per log, versus P137 early0, for every simple destination mode; exception frame, rollback and sentinel assertions pass. Earlier `faultroot_s*` runs opened a nonexistent `test.hex` and failed; they are invalid, not fallback coverage. The corrected runs use the individually named source/destination fixture images.

Reusable runner validation: `pipeline_memmove_values.py --cache SNAPSHOT --simple-destination {2,3,4} --handoff-monitor exact --stack-register {source,destination}` passes all 12 P137/P146 matrices under `scratch/p146_repro_values_20260921`; add `--require-early` for P146. Root verified every log and source hash. A7 source/destination and aliases retain expected values, byte step, guards and flags; each candidate matrix records early324/checked324, baseline0/0. The runner records explicit source identities and rejects FAIL/FATAL/ERROR logs.
