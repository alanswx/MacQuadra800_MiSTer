# P130 spanning-read data selection timing experiment

The completed P120/P124 fit uses 39,795 ALMs, 25,702 registers, 482 RAM blocks and 42 DSPs. CPU setup slack is -0.188 ns, RAM +0.483 ns and HDMI +0.323 ns. The worst CPU path runs from tc[14] through MMU request qualification, fast_span_ack, pipeline load data/flags and branch-prefetch seeding to epf_data[5][15]. This is not the older PEA address ALU timing path.

P130 leaves acknowledgement unchanged but selects the spanning read data from registered transaction state (C_LOOK, look2, r_span2, data bank). Request/snoop/error checks still qualify acceptance; they no longer select the data mux. Intended effect is timing only, without changing accepted data or cycle count. Source `scratch/p130_span_data_select_20260921/ap040_cache.v`; unapplied patch `scripts/cpu/span_data_select.patch` over P124. Simulation, exact-ack/error/CE/snoop checks and a new fit remain required. No timing improvement is claimed until measured.

Promoted for FPGA timing evaluation after root audited `scratch/p130_full_integration_20260921`: all 22 program tests and four IRQ/replay tests pass, HANDOFF accounting and oracle trace match, and recorded source hashes match. Baseline P124 and candidate P130 both pass portable exact-span/no-gap tests (13 fast spanning acknowledgements), cache snoop tests, 100 XSTORE cases and the 216-case posted-read matrix. The pipeline prototype prerequisite uses the unchanged production P120 module. No Quartus flow was live before promotion. Cache SHA256 94e164382a6eab533cb02b035882883c96cf65f615e0b86db8e8c1ae777fbf7d. FPGA timing improvement remains unproven.

Fit launched from commit 31ff2a5: unit `q800-p130-span-data-timing-fit-20260921.service`, wrapper PID 275474 and Quartus flow PID 275498 verified live. Archive `scratch/p130_span_data_timing_fit_20260921`, seed 23, CD-ROM/audio and Ethernet omitted for development. Tracked HDL/QSF/QIP/SDC are frozen through the entire wrapper. The wrapper records source identities, fit/RAM/timing reports and RBF hash; copy the two cross-domain reports from scratch into the archive after completion. No fitted result yet.

Seed 23 terminated with routing congestion and fitter failure (build.exit=3, source_check.exit=0) after 13m56s. No new RBF or timing analysis was produced. Failed-fit resource report: 39,920 ALMs, 25,108 fitted registers, 482 RAM blocks, 42 DSPs. The -0.188 ns timing report printed by the build wrapper is stale P120/P124 evidence and does not describe P130. A single seed-24 retry is prepared with unchanged P130 RTL; the prior full wrapper is terminal.
