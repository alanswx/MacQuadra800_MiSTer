# P108 displacement destination at MOVE read acknowledgement

Isolated candidate over qualified P106, production unchanged during its fit.
Unapplied patch scripts/cpu/move_destination_d16_ack.patch.
Candidate scratch/p108_move_d16_base_20260921/ap040_core.v.

Select d16 destination An while the source read waits. At a successful
ordinary source acknowledgement, if the destination extension is resident,
the base selector matches and neither register write port is pending,
consume the extension and calculate the destination address together.
Enter S_PIPE_DEA, which still handles MOVE flags and the ordered store.
Otherwise retain the existing immf/S_EA_D16 path. Source errors and split
reads retain their original paths. No destination memory request occurs
before the successful source acknowledgement.

Whetstone, value/alias, extension fault, destination fault and interrupt
boundary screening pending. Not qualified or applied.

## Initial failure and corrected screen

Initial P108 failed Whetstone and directed value/boundary/destination-fault
checks. The deferred immf task set igo even on the new direct path; after
the state case, immf_now overrode S_PIPE_DEA and fetched another extension
after the shortcut had already consumed one. The initial logs remain under
scratch/p108_move_d16_base_20260921. The Whetstone failure time in its initial
summary was mislabeled as CPU cycles; no valid completion count exists.

Corrected P108b calls immf only in the fallback branch. Candidate is now
scratch/p108b_move_d16_base_20260921/ap040_core.v; the unapplied patch reflects
this correction. Luna is rerunning the same gates; not qualified or applied.

## P108b initial screen

Corrected core SHA256 b875151ebd0a9914233ccbdd758301ceb27de444c24dea5aeeecc389eabdab4e.
Full Whetstone loop clocks 29,454,150 -> 29,050,125 (1.372% fewer), with
identical stack/globals/code captures and matching fixture/ROM/flags/supporting
RTL. Root ran compare_whetstone_runs.py; comparison.json is under
scratch/whetstone_full_p108b_20260921. Numerical oracle remains pending.

Displacement destination values, source/destination/extension faults and
IRQ/T1/alias/split-source boundaries pass. Broader integration/corpus and
new signed-displacement MMU repair/RTE tests are assigned to Luna.
Production remains P106 while its Quartus fit runs.

Signed d16 MMU fault-repair/RTE matrix passes for both P106 and P108b at
-16 and +16: 12 cases per invocation (B/W/L, source post/pre, source/dest
fault), each in three bus phases. Root inspected all 48 completed logs.
The new --displacement-destination option in move_simple_restart.py preserves
the original simple-mode matrix when omitted. Artifacts under
scratch/p108b_move_d16_base_20260921/restart.

## Broader qualification complete

Extended integration passes through final IRQ/replay/ownership cases.
First-100 corpus with verified b875151e... core and 9b0e3e00... pipeline
passes all 1,900 field groups, zero real differences, 28,483,982 cycles.
Correct final artifacts /tmp/cpu-corpus100-gate.C62dHT, recorded in
scratch/p108b_move_d16_base_20260921/corpus100.log. This is not the full corpus.
P108b is ready for promotion after the active P106 fit's complete wrapper;
no production change has been made during the build.

After P106's complete fit/STA/archive, the exact qualified P108b core was
applied to production. move_destination_d16_ack.patch is historical/APPLIED.
Pipeline/supporting RTL unchanged; next single fit retains seed23.

## FPGA result

Seed23 development fit completed in 15m47s. Quartus compilation succeeded;
build_only.sh returned1 for failed timing. The complete wrapper is terminal;
source identity, cross-domain STA and detailed CPU STA checks each exited0.
CPU setup +0.079 ns, RAM +0.562 ns, HDMI -0.305 ns. Crossing minima:
sys-to-RAM +0.822 ns; RAM-to-sys +0.313 ns. Uses39,724ALMs,
25,667registers,482RAMblocks,42DSPblocks. CPU timing passes, HDMI timing
precludes release qualification; development hardware testing is authorized.

Immutable archive scratch/p108bd16_fit_20260921:
MacQuadra800_p108bd16_ea986aa.rbf, SHA256
44f8339cb6dfb8a60dba73079fa539907d190c85feecef1397b0e7ca2f197cbe.
Reports and complete source manifest/checks are archived beside the bitstream.
CD-ROM/audio and Ethernet are omitted. Hardware boot succeeded; the five-run
series and explicit application-quit/shutdown check remain pending.
