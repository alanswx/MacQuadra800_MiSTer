# P161 consume the retirement-time read request

Unqualified correction over P159 (not P160). Source `scratch/p161_alu_read_dispatch_20260921/ap040_core.v`, SHA256 `a0d6053901b044128d9c6ff1513f19d2cc9318ad3285b1ddd226809022359770`; patch `scripts/cpu/alu_read_late_dispatch.patch` applies to P159.

The lookahead apply_record site executes after the common mgo dispatcher. P159's late mrd was therefore never consumed; the following instruction returned through decode instead of launching its read. P161 calls mem_issue immediately after that late mrd, then clears mgo. This is restricted to the same address-register ALU producer and following indirect memory-MOVE dependency. It does not move the global dispatcher or duplicate its earlier calls. Existing RAM/page/port guards remain inside mem_issue.

Original Whetstone screening and a directed before/after-edge forwarding fixture are running with explicitP120/P136. Require positive actual S_PIPE_REGS-to-S_MRD request coverage, correct new-versus-old address selection, flags/guards, and then faults/boundaries/full integration if worthwhile. No correctness, speed, timing or hardware claim yet. Production remains frozen for P150.

Original Whetstone now improves to25,247,483 loop/25,248,111 returned cycles,82,924 fewer loop cycles (0.327%) versus P154. Root verified all three captures, non-core identities and every source hash (`scratch/whetstone_full_p161_20260921/root_audit.txt`). This corrected variant, unlike P159/P160, consumes the new memory request. Dhrystone/full integration remain in progress.

Directed value fixture with caches enabled passesP154 andP161, with edge-correlated forwarding counts0 versus63 across3busphases. Root audited clean logs/source hashes; distinct wrong-old-address payloads, result/CCR/A0/A7 and adjacent guard assertions pass. The fixture preserves immediate ALU-producer-to-MOVE adjacency and allows prefetch before the producer. Earlier cache-off fixtures failed required path coverage, despite passing values; those remain excluded. Preserved `scripts/cpu/alu_move_forward_values.s` and monitor `scripts/cpu/alu_move_forward_monitor.sv.inc` (topforward_monitor, plusargrequire_baseline/require_early), evidence `scratch/p161_forward_cached_runs_20260921`. Source fault preserving prior An commit is next; precise faults/boundaries remain unqualified.

Original Dhrystone remains104,254,232 loop/104,255,122 returned cycles, unchanged versusP154. Root verified all five captures, non-core identities and all source hashes, then independently ran the50,000-iteration checker:PASS (`scratch/dhrystone_full_p161_20260921/root_audit.txt`).

Source-fault qualification is still open. Initial and root-created same-site warmed fixtures preserve the expected architecture but record zero actual early-read/fault-forwarding events. Candidate required-coverage assertions therefore fail; these are not accepted as proof of the changed fault path. A separate agent-generated warm fixture also had mismatched A1 setup/expectation and did not actually warm the producer/read instruction site; it is excluded. Root-created evidence lives under `scratch/p161_warm_fault_root_runs_20260921`; predicate diagnostics are requested before further changes.

Full P161/P120/P136 CPU integration independently audited by root:22programs+4IRQ/replay cases pass, balanced handoff accounting, exact oracle trace and all source hashes match (`scratch/p161_full_integration_20260921/root_audit.txt`). Dedicated changed-path fault/boundary checks remain separate.

Exact predicate diagnostics show the warmed source-fault producer5548 followed by2290 satisfies the forwarding guard. The agent's earlier interpretation of p_src4 as memory was incorrect:4 is SK_IMPL for the quick arithmetic producer; the next instruction has n_p_src3(SK_MEM), n_p_dst2(DK_MEM), as required. Predicate truth does not prove actual transition. A root monitor now separately checks producer-to-S_MRD addressF140 preparation, immediate request issue and the later actual error response, because existing coverage required immediate issue and could miss a correctly forwarded but deferred read.

Changed-path source bus fault now proven in the same asserted fixture: P154 prepared0/immediate0/errors3; P161 prepared3/immediate0/errors3. Root audited both clean logs and every recorded input hash (`scratch/p161_fault_correlated_runs_20260921`). Candidate transitions directly from the ALU producer into S_MRD atF140, then issues later and receives the expected fault. SavedPC/SR/format7/faultaddress, committed prior A0=f140 and unchanged destination checks all pass. Immediate issue is explicitly zero, not claimed. Preserved fixture `scripts/cpu/alu_move_forward_source_fault.s`, monitor `scripts/cpu/alu_move_forward_fault_monitor.sv.inc` topfault_correlation_monitor, plusargrequire_baseline/require_fault. MMU/source split, destination faults and trace/IRQ remain directed follow-ups.

Successful source page-crossing checks now pass on P154/P161 with explicitP120/P136 under4K and8K MMU pages. Source1ffe crosses2000; payload/CCR/An/destination/source and adjacent guards are asserted. Root audited all four logs and source hashes (`scratch/p161_split_read_runs_20260921`). Baseline preparation0, candidate24 across3phases; split-state counts469(4K)/448(8K) match baseline; immediateissue0 as required by existing page guard. Preserved `scripts/cpu/alu_move_forward_split_{4k,8k}.s` and `scripts/cpu/alu_move_forward_split_monitor.sv.inc`, topsplit_forward_monitor. Invalid-page source-fault variants remain pending; the new duplicate off-MMU busfault run is not evidence for them.

Invalid-page source faults now pass for4K/8K MMU pages on P154/P161, explicitP120/P136. Root audited all four clean logs and source hashes (`scratch/p161_mmu_fault_root_runs_20260921`). Page2000 is invalid; warm sourceC000 and destinationD000 are mapped. Baseline prepared0/immediate0/errors3; candidate prepared3/immediate3/errors3. SavedPC/SR/format7/address, prior committedA0=2000 and preserved memory are asserted. This supplies immediate-request fault coverage in addition to the deferred busfault case. Preserved fixtures `scripts/cpu/alu_move_forward_mmu_fault_{4k,8k}.s` and corresponding `.sv.inc` monitor. Destination faults and trace/IRQ remain directed follow-ups.

P161/P151 original workload screens audited: Whetstone25,366,469/25,367,097 cycles and Dhrystone104,254,232/104,255,122. Root compared all eight captures and non-cache identities against P161/P136, rehashed all sources and independently ran the50,000-iteration Dhrystone checker:PASS. Actual evidence directories use p151b in their names: `scratch/{whetstone,dhrystone}_full_p161_p151b_20260921/root_audit.txt`; the unsuffixed Whetstone directory lacks a completed manifest and is not accepted. Cache source hash matches P151. Shared cache costs118,986 Whetstone clocks versus P136, but P161/P151 still saves43,930 versus P154/P151. Combined full integration is running; no FPGA/hardware claim.

T1 producer trace boundary passes onP154/P161 with explicitP120/P136. RTE enables tracing for the warmed address-register producer; the handler asserts format2, following-instruction PC, committed An and untouched destination. Root audited clean logs/all source hashes (`scratch/p161_trace_vector_runs_20260921`). Both cores observe3tracedproducers and0followingreads. Preserved `scripts/cpu/alu_move_forward_trace.s` and `.sv.inc` monitor toptrace_forward_monitor. Original backward-org vector construction failed assembly and is excluded; the accepted fixture uses ordered vector entries. This is T1 coverage, not IRQ or destination-fault coverage.

Combined P161/P120/P151 integration is now independently audited: all 22 CPU programs and four IRQ/replay suites pass, each handoff count balances commits plus cancellations, the architectural trace exactly matches the oracle, and every recorded source hash was revalidated. Evidence: `scratch/p161_p151_full_integration_20260921/root_audit.txt`. Dedicated forwarding destination-fault and IRQ tests remain pending; generic integration does not substitute for those changed-path checks. No new hardware score is claimed.
