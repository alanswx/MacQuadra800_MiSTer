# P161 consume the retirement-time read request

Unqualified correction over P159 (not P160). Source `scratch/p161_alu_read_dispatch_20260921/ap040_core.v`, SHA256 `a0d6053901b044128d9c6ff1513f19d2cc9318ad3285b1ddd226809022359770`; patch `scripts/cpu/alu_read_late_dispatch.patch` applies to P159.

The lookahead apply_record site executes after the common mgo dispatcher. P159's late mrd was therefore never consumed; the following instruction returned through decode instead of launching its read. P161 calls mem_issue immediately after that late mrd, then clears mgo. This is restricted to the same address-register ALU producer and following indirect memory-MOVE dependency. It does not move the global dispatcher or duplicate its earlier calls. Existing RAM/page/port guards remain inside mem_issue.

Original Whetstone screening and a directed before/after-edge forwarding fixture are running with explicitP120/P136. Require positive actual S_PIPE_REGS-to-S_MRD request coverage, correct new-versus-old address selection, flags/guards, and then faults/boundaries/full integration if worthwhile. No correctness, speed, timing or hardware claim yet. Production remains frozen for P150.

Original Whetstone now improves to25,247,483 loop/25,248,111 returned cycles,82,924 fewer loop cycles (0.327%) versus P154. Root verified all three captures, non-core identities and every source hash (`scratch/whetstone_full_p161_20260921/root_audit.txt`). This corrected variant, unlike P159/P160, consumes the new memory request. Dhrystone/full integration remain in progress.

Directed value fixture with caches enabled passesP154 andP161, with edge-correlated forwarding counts0 versus63 across3busphases. Root audited clean logs/source hashes; distinct wrong-old-address payloads, result/CCR/A0/A7 and adjacent guard assertions pass. The fixture preserves immediate ALU-producer-to-MOVE adjacency and allows prefetch before the producer. Earlier cache-off fixtures failed required path coverage, despite passing values; those remain excluded. Preserved `scripts/cpu/alu_move_forward_values.s` and monitor `scripts/cpu/alu_move_forward_monitor.sv.inc` (topforward_monitor, plusargrequire_baseline/require_early), evidence `scratch/p161_forward_cached_runs_20260921`. Source fault preserving prior An commit is next; precise faults/boundaries remain unqualified.

Original Dhrystone remains104,254,232 loop/104,255,122 returned cycles, unchanged versusP154. Root verified all five captures, non-core identities and all source hashes, then independently ran the50,000-iteration checker:PASS (`scratch/dhrystone_full_p161_20260921/root_audit.txt`).

Source-fault qualification is still open. Initial and root-created same-site warmed fixtures preserve the expected architecture but record zero actual early-read/fault-forwarding events. Candidate required-coverage assertions therefore fail; these are not accepted as proof of the changed fault path. A separate agent-generated warm fixture also had mismatched A1 setup/expectation and did not actually warm the producer/read instruction site; it is excluded. Root-created evidence lives under `scratch/p161_warm_fault_root_runs_20260921`; predicate diagnostics are requested before further changes.
